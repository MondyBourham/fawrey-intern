You need to make the script executable by 
chmod +x systemd-dns.sh nm-dns.sh 

Run what is suitable for you 
./systemd-dns.sh 
./nm-dns.sh (here before you may need to create an active connection to be used here)